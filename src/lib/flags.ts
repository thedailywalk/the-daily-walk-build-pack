/**
 * Feature switches.
 *
 * GOOD_NEWS_ENABLED — master switch for everything that republishes Good News
 * Network stories (the homepage "Good news" section, the /good-news reading
 * room, and the "Good News" nav link). Turned OFF until we have permission
 * from Good News Network to feature their content. Flip to `true` to re-enable.
 */
export const GOOD_NEWS_ENABLED = false;
